name: Create Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: write

jobs:
  call-shared-workflow:
    uses: RemRemRemRe/.github/workflow/create-release-notes/workflows/create-release-notes.yml@main
    secrets: inherit
